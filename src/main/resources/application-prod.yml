spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: jdbc:${DATABASE_URL}

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    hibernate:
      ddl-auto: update

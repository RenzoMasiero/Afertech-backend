spring:
  application:
    name: afertech

  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: validate

  flyway:
    baseline-on-migrate: true

server:
  port: 8080

jwt:
  expiration: 3600000